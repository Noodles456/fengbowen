<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fbw.OneBoot.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="com.fbw.OneBoot.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Apr 06 17:41:31 CST 2020.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
    <result column="creator" jdbcType="INTEGER" property="creator" />
    <result column="comment_count" jdbcType="INTEGER" property="commentCount" />
    <result column="view_count" jdbcType="INTEGER" property="viewCount" />
    <result column="like_count" jdbcType="INTEGER" property="likeCount" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
  </resultMap>
  <update id="inCView" parameterType="Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Apr 06 17:41:31 CST 2020.
    -->
    update question
    set
    view_count =  view_count + #{viewCount,jdbcType=INTEGER}
    where id=#{id}
  </update>
    <update id="commentCount" parameterType="question">
 update question
    set
    comment_count =  comment_count + #{commentCount,jdbcType=INTEGER}
    where id=#{id}
    </update>
    <select id="selectRelated" parameterType="question" resultMap="BaseResultMap">
    select * from question where id != #{id} and tag regexp #{tag}
    </select>
    <select id="countBySearch" parameterType="com.fbw.OneBoot.dto.SearchDTO" resultType="java.lang.Integer">
    select count(*) from question
        <where>
            <if test="search != null and search != ''">
                and  title regexp #{search}
            </if>
         <if test="zero != null and zero != ''">
             and comment_count regexp #{zero}
         </if>
            <if test="hot != null and hot != ''">
                and tag regexp #{hot}
            </if>
        </where>

    </select>

    <select id="selectBySearch" parameterType="com.fbw.OneBoot.dto.SearchDTO" resultMap="BaseResultMap">
        select * from question
        <where>
            <if test="search != null and search != '' ">
                and  title regexp #{search}
            </if>
            <if test="zero != null and zero != ''">
                and comment_count regexp #{zero}
            </if>
            <if test="hot != null and hot != ''">
                and tag regexp #{hot}
            </if>
        </where>
 order by gmt_create desc limit #{page},#{size}
    </select>

</mapper>